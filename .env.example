# Server port. Optional: defaults to 3000 if unset.
# On Render you don't need to set it: the platform injects PORT automatically.
PORT=3000

# Path to the players .sqlite. Optional: defaults to data/futbolle.sqlite.
# DB_PATH=./data/futbolle.sqlite
